apply plugin: 'java'
apply plugin: 'application'

repositories {
    mavenCentral()
}

group 'com.uksf.mm'

//Dependencies
dependencies {
    compile fileTree(dir: 'lib', include: '*.jar')
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.4'
}