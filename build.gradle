group 'com.uksf.tim'
version '0.1'

apply plugin: 'application'
mainClassName = 'com.uksf.tim.Run'
task wrapper(type: Wrapper) {
    gradleVersion = '2.12'
}
dependencies {
    compile fileTree(dir: 'lib', include: '*.jar')
}